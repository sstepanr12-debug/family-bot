{
  "name": "family-calendar",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run db:generate -w backend && npm run build -w frontend && npm run build -w backend",
    "test": "npm run test -w backend",
    "db:push": "npm run db:push -w backend",
    "start": "npm run db:push -w backend && npm run start -w backend",
    "db:postgres": "npm run db:postgres -w backend",
    "db:sqlite": "npm run db:sqlite -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}